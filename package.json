{
  "name": "clyde",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "workspaces": [
    "clyde-bot",
    "clyde-dashboard",
    "clyde-server"
  ],
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
