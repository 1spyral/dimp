{
  "name": "clyde",
  "private": true,
  "scripts": {
    "docker:build:bot": "docker build -f clyde-bot/Dockerfile -t clyde-bot .",
    "docker:build:dashboard": "docker build -f clyde-dashboard/Dockerfile -t clyde-dashboard .",
    "docker:build:server": "docker build -f clyde-server/Dockerfile -t clyde-server .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "workspaces": [
    "clyde-auth",
    "clyde-bot",
    "clyde-dashboard",
    "clyde-server"
  ],
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
