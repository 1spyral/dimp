{
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "workspaces": [
    "clyde-bot",
    "clyde-dashboard",
    "clyde-server"
  ]
}
